# -----------------------------------------------------------------------------
# For live deploy we keep in repo: app/, src/, scripts/, data/*.csv,
# models/general_predictor.pkl, models/training_summary.json, requirements.txt
# -----------------------------------------------------------------------------

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
.cursor/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Optional live cache (not required for deploy)
data/live/
!data/live/.gitkeep

# Trained models: commit models/*.pkl and models/training_summary.json so the live app can load them
# models/models_real/ kept if you use that path
models/models_real/

# Logs
*.log
logs/

# Personal / reference only (not needed to run or deploy the app)
RESUME_PROJECT.md

# Secrets (use .env.example as template)
.env
.env.local
.env.production
*.pem

# Temporary / one-off scripts
*.tmp
*.temp
*.swp
*.swo
*~
temp/
tmp/
fix_*.py
scratch*.py
