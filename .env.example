# Fantasy Football Predictor - copy to .env and adjust for your environment
# For Docker Compose, these are set in docker-compose.yml; override here if needed.

# PostgreSQL (used by web app and API when USE_DB=true)
DATABASE_URL=postgresql://fantasy_user:fantasy_password@localhost:5432/fantasy_football
# In Docker, use: postgresql://fantasy_user:fantasy_password@postgres:5432/fantasy_football

# Redis (cache layer)
REDIS_URL=redis://localhost:6379/0
# In Docker, use: redis://redis:6379

# Set to false to skip Postgres/Redis and use CSV only (e.g. local dev without Docker)
USE_DB=true

# Optional: cache TTL in seconds (default 300)
# CACHE_TTL_SECONDS=300
