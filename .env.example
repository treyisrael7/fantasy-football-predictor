# Fantasy Football Forecasting Dashboard - copy to .env if you need env vars
# The app runs from CSV by default; no .env is required for local use.

# Optional: set to true only if you run Postgres + Redis yourself (not included in this repo).
USE_DB=false
